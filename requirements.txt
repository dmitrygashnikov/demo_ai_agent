# --- Core orchestration ---
langgraph==0.2.60
langgraph-checkpoint-postgres==2.0.7
langchain-core==0.3.28
# langchain is required by the Langfuse LangChain CallbackHandler used to trace
# the LangGraph run (see app/observability/langfuse_client.py).
langchain==0.3.13

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.1
python-multipart==0.0.20

# --- LLM / Embeddings (OpenAI-compatible protocol) ---
openai==1.59.5

# --- Vector store ---
qdrant-client==1.12.2

# --- Relational DB ---
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
psycopg[binary]==3.2.3

# --- Cache / sessions / queue ---
redis==5.2.1

# --- Config & validation ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Sandbox / code execution ---
docker==7.1.0

# --- HTTP client (RapidAPI, external APIs) ---
httpx==0.28.1
tenacity==9.0.0

# --- Observability (optional LangGraph tracing via Langfuse) ---
langfuse==2.59.7

# --- Authentication (application JWT auth + password hashing) ---
passlib[bcrypt]==1.7.4
# Pinned to 4.0.1: passlib 1.7.4 reads bcrypt.__about__ which was removed in
# bcrypt>=4.1, producing a harmless but noisy startup warning. 4.0.1 is clean.
bcrypt==4.0.1
PyJWT==2.10.1
# Pydantic EmailStr validation for the auth request models.
email-validator==2.2.0
